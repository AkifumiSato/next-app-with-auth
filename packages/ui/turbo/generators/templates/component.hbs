import * as React from "react";

type Props = {
  children?: React.ReactNode;
}

export function {{ pascalCase name }}({ children }: Props): React.JSX.Element {
  return (
    <div>
      <h1>{{ name }}</h1>
      {children}
    </div>
  );
};
